#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, m;
    cin >> n;
    int arr[n];
    for (int i = 0; i < n; ++i) {
        cin >> arr[i];
    }
    cin >> m;
    int brr[m];
    for (int i = 0; i < m; ++i) {
        cin >> brr[i];
    }
    int j = 0;
    for (int i = 0; i < min(n, m); ++i) {
        while (  

    return 0;
}
